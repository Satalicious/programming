#include <stdio.h>

typedef struct node Node;
typedef struct linkedList LinkedList;

struct node {
    int data;
    Node *next;
    Node *prev;
};

struct linkedlist {
    Node head;
    Node tail;
};

void push_front() {

}

int main(int argc, char** argv) {
    LinkedList *list;

return 0;
}