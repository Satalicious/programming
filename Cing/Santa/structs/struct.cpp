

#include <stdio.h>

typedef struct {
    float x, y;
}

void centroid(Point A, Point B, Point C) {
    float x_G = 1.0 / 3.0 * (A.x - B.x);
    float y_G = 1.0 / 3.0 * (A.y - B.y);
    G = {x_G, y_G};
    return G;
}

int main() {
A = { 3.0,2.1 }, B = { 1.2, 3.4}, C = { 1.2, 8.7 };
centroid(3.0, 3.0, 3.0);
return 0;
}